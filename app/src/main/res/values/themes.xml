<resources>
    <!-- Tu tema principal de la app (ya debería existir) -->
    <style name="Theme.DriverApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- ... -->
    </style>

    <!-- 👇 ESTILO AÑADIDO PARA LA SPLASH SCREEN 👇 -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- El fondo de la splash screen -->
        <item name="windowSplashScreenBackground">#FFFFFF</item> <!-- Cambia #FFFFFF por el color de fondo que desees -->

        <!-- El ícono que se mostrará mientras carga la animación. Puede ser tu logo estático. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>

        <!-- El tema al que se cambiará cuando la splash screen termine -->
        <item name="postSplashScreenTheme">@style/Theme.DriverApp</item>
    </style>
</resources>
